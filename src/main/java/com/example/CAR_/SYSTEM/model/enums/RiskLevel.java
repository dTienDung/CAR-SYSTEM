package com.example.CAR_.SYSTEM.model.enums;

public enum RiskLevel {
    CHAP_NHAN,      // Chấp nhận (< 15 điểm)
    XEM_XET,        // Xem xét (15-24 điểm)
    TU_CHOI         // Từ chối (>= 25 điểm)
}

